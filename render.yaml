services:
  - type: web
    name: fractureai
    runtime: python
    plan: free
    pythonVersion: "3.11.9"
    buildCommand: |
      pip install -r requirements.txt
      mkdir -p ml/models
      curl -L -o ml/models/best.pt https://github.com/BackSua/FRACTURAS-IA/releases/download/v1.0/best.pt
      DJANGO_SETTINGS_MODULE=fracture_ai.settings_prod python manage.py collectstatic --noinput
      DJANGO_SETTINGS_MODULE=fracture_ai.settings_prod python manage.py migrate
    startCommand: gunicorn fracture_ai.wsgi:application --workers 1 --timeout 120 --bind 0.0.0.0:$PORT
    envVars:
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: DJANGO_SETTINGS_MODULE
        value: fracture_ai.settings_prod
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: PYTHONDONTWRITEBYTECODE
        value: "1"
      - key: PYTHONUNBUFFERED
        value: "1"
